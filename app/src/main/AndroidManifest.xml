<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.a98611.test">

    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.CALL_PHONE" />
    <uses-permission android:name="android.permission.CALL_PHONE" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SpliashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MyCustom" />
        <activity
            android:name=".LockActivity"
            android:launchMode="singleInstance" />
        <activity android:name=".UI.WaterListview" />
        <activity android:name=".MyObjectAnimation" />
        <activity android:name=".LoadMoreActivity" />

        <service android:name=".service.MyService">
            <intent-filter>
                <action android:name="com.cn" />
            </intent-filter>
        </service>
        <service android:name=".service.RockService" />

        <activity android:name=".MainActivity"/>
    </application>

</manifest>